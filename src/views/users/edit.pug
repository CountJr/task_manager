extends /layouts/application.pug

block contents
  +formFor(f, { url: urlFor('settings'), method: 'post' })
    +input(f, 'email')
    +input(f, 'alias')
    +input(f, 'firstname')
    +input(f, 'lastname')
    +input(f, 'oldPassword', { as: 'password' })
    +button(f)
